#lang racket/base
;;; Autogenerated on Tuesday, October 18th, 2022 1:50:04am
;;; DO NOT EDIT

(require (only-in
          "base.rkt"
          s64?
          s64vector?
          s64vector
          make-s64vector
          s64vector-ref
          s64vector-set!
          s64vector-length
          s64vector->list
          list->s64vector)
         (only-in "../128.rkt" comparator? make-comparator)
         racket/contract)

(module+ test (require rackunit))

(provide (all-from-out "base.rkt")
         s64vector-append-subvectors
         (contract-out
          (s64vector-unfold
           (-> procedure? exact-nonnegative-integer? any/c s64vector?))
          (s64vector-unfold-right
           (-> procedure? exact-nonnegative-integer? any/c s64vector?))
          (s64vector-copy
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            s64vector?))
          (s64vector-copy!
           (->*
            (s64vector? exact-nonnegative-integer? s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            void?))
          (s64vector-reverse-copy
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            s64vector?))
          (s64vector-reverse-copy!
           (->*
            (s64vector? exact-nonnegative-integer? s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            void?))
          (s64vector-append (-> s64vector? ... s64vector?))
          (s64vector-concatenate (-> (listof s64vector?) s64vector?))
          (s64vector-empty? (-> s64vector? boolean?))
          (s64vector= (-> s64vector? s64vector? s64vector? ... boolean?))
          (s64vector-take (-> s64vector? exact-nonnegative-integer? s64vector?))
          (s64vector-take-right
           (-> s64vector? exact-nonnegative-integer? s64vector?))
          (s64vector-drop (-> s64vector? exact-nonnegative-integer? s64vector?))
          (s64vector-drop-right
           (-> s64vector? exact-nonnegative-integer? s64vector?))
          (s64vector-segment
           (-> s64vector? exact-positive-integer? (listof s64vector?)))
          (s64vector-fold
           (->
            (-> s64? s64? ... any/c any/c)
            any/c
            s64vector?
            s64vector?
            ...
            any/c))
          (s64vector-fold-right
           (->
            (-> s64? s64? ... any/c any/c)
            any/c
            s64vector?
            s64vector?
            ...
            any/c))
          (s64vector-map
           (-> (-> s64? s64? ... s64?) s64vector? s64vector? ... s64vector?))
          (s64vector-map!
           (-> (-> s64? s64? ... s64?) s64vector? s64vector? ... s64vector?))
          (s64vector-for-each
           (-> (-> s64? s64? ... s64?) s64vector? s64vector? ... void?))
          (s64vector-count
           (->
            (-> s64? s64? ... any/c)
            s64vector?
            s64vector?
            ...
            exact-nonnegative-integer?))
          (s64vector-cumulate
           (-> (-> any/c s64? s64?) any/c s64vector? s64vector?))
          (s64vector-take-while (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-take-while-right
           (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-drop-while (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-drop-while-right
           (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-index
           (->
            (-> s64? s64? ... any/c)
            s64vector?
            s64vector?
            ...
            (or/c exact-nonnegative-integer? #f)))
          (s64vector-index-right
           (->
            (-> s64? s64? ... any/c)
            s64vector?
            s64vector?
            ...
            (or/c exact-nonnegative-integer? #f)))
          (s64vector-skip
           (->
            (-> s64? s64? ... any/c)
            s64vector?
            s64vector?
            ...
            (or/c exact-nonnegative-integer? #f)))
          (s64vector-skip-right
           (->
            (-> s64? s64? ... any/c)
            s64vector?
            s64vector?
            ...
            (or/c exact-nonnegative-integer? #f)))
          (s64vector-any
           (-> (-> s64? s64? ... any/c) s64vector? s64vector? ... any))
          (s64vector-every
           (-> (-> s64? s64? ... any/c) s64vector? s64vector? ... any))
          (s64vector-partition
           (->
            (-> s64? any/c)
            s64vector?
            (values s64vector? exact-nonnegative-integer?)))
          (s64vector-filter (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-remove (-> (-> s64? any/c) s64vector? s64vector?))
          (s64vector-swap!
           (->
            s64vector?
            exact-nonnegative-integer?
            exact-nonnegative-integer?
            void?))
          (s64vector-fill!
           (->*
            (s64vector? s64?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            void?))
          (s64vector-reverse!
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            void?))
          (s64vector-unfold!
           (->
            (-> exact-nonnegative-integer? any/c (values s64? any/c))
            s64vector?
            exact-nonnegative-integer?
            exact-nonnegative-integer?
            any/c
            void?))
          (s64vector-unfold-right!
           (->
            (-> exact-nonnegative-integer? any/c (values s64? any/c))
            s64vector?
            exact-nonnegative-integer?
            exact-nonnegative-integer?
            any/c
            void?))
          (reverse-s64vector->list
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            (listof s64?)))
          (reverse-list->s64vector (-> (listof s64?) s64vector?))
          (s64vector->vector
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            (vectorof s64?)))
          (vector->s64vector
           (->*
            ((vectorof s64?))
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            s64vector?))
          (make-s64vector-generator
           (->*
            (s64vector?)
            (exact-nonnegative-integer? exact-nonnegative-integer?)
            (-> (or/c s64? eof-object?))))
          (write-s64vector (->* (s64vector?) (output-port?) void?))
          (s64vector-comparator comparator?)))

(define (s64vector-unfold f len seed)
  (let ((v (make-s64vector len)))
    (let loop ((i 0) (state seed))
      (unless (= i len)
        (let-values (((value newstate) (f i state)))
          (s64vector-set! v i value)
          (loop (+ i 1) newstate))))
    v))

(define (s64vector-unfold-right f len seed)
  (let ((v (make-s64vector len)))
    (let loop ((i (- len 1)) (state seed))
      (unless (= i -1)
        (let-values (((value newstate) (f i state)))
          (s64vector-set! v i value)
          (loop (- i 1) newstate))))
    v))

(define (s64vector-copy vec (start 0) (end (s64vector-length vec)))
  (let ((v (make-s64vector (- end start))))
    (s64vector-copy! v 0 vec start end)
    v))

(define (s64vector-copy! to at from (start 0) (end (s64vector-length from)))
  (let loop ((at at) (i start))
    (unless (= i end)
      (s64vector-set! to at (s64vector-ref from i))
      (loop (+ at 1) (+ i 1)))))

(define (s64vector-reverse-copy vec (start 0) (end (s64vector-length vec)))
  (let ((v (make-s64vector (- end start))))
    (s64vector-reverse-copy! v 0 vec start end)
    v))

(define (s64vector-reverse-copy!
         to
         at
         from
         (start 0)
         (end (s64vector-length from)))
  (let loop ((at at) (i (- end 1)))
    (unless (< i start)
      (s64vector-set! to at (s64vector-ref from i))
      (loop (+ at 1) (- i 1)))))

(define (s64vector-append . vecs) (s64vector-concatenate vecs))

(define (s64vector-concatenate vecs)
  (let ((v (make-s64vector (len-sum vecs))))
    (let loop ((vecs vecs) (at 0))
      (unless (null? vecs)
        (let ((vec (car vecs)))
          (s64vector-copy! v at vec 0 (s64vector-length vec))
          (loop (cdr vecs) (+ at (s64vector-length vec)))))
      v)))

(define (len-sum vecs)
  (if (null? vecs) 0 (+ (s64vector-length (car vecs)) (len-sum (cdr vecs)))))

(define (s64vector-append-subvectors . args)
  (let ((v (make-s64vector (len-subsum args))))
    (let loop ((args args) (at 0))
      (unless (null? args)
        (let ((vec (car args)) (start (cadr args)) (end (caddr args)))
          (s64vector-copy! v at vec start end)
          (loop (cdddr args) (+ at (- end start))))))
    v))

(define (len-subsum vecs)
  (if (null? vecs)
    0
    (+ (- (caddr vecs) (cadr vecs)) (len-subsum (cdddr vecs)))))

(define (s64vector-empty? vec) (zero? (s64vector-length vec)))

(define (s64vector= vec1 vec2 . vecs)
  (and (s64dyadic-vecs=
        vec1
        0
        (s64vector-length vec1)
        vec2
        0
        (s64vector-length vec2))
       (or (null? vecs) (s64vector= vec2 (car vecs) (cdr vecs)))))

(define (s64dyadic-vecs= vec1 start1 end1 vec2 start2 end2)
  (cond
   ((not (= end1 end2)) #f)
   ((not (< start1 end1)) #t)
   ((let ((elt1 (s64vector-ref vec1 start1)) (elt2 (s64vector-ref vec2 start2)))
      (= elt1 elt2))
    (s64dyadic-vecs= vec1 (+ start1 1) end1 vec2 (+ start2 1) end2))
   (else #f)))

(define (s64vector-take vec n)
  (let ((v (make-s64vector n))) (s64vector-copy! v 0 vec 0 n) v))

(define (s64vector-take-right vec n)
  (let ((v (make-s64vector n)) (len (s64vector-length vec)))
    (s64vector-copy! v 0 vec (- len n) len)
    v))

(define (s64vector-drop vec n)
  (let* ((len (s64vector-length vec))
         (vlen (- len n))
         (v (make-s64vector vlen)))
    (s64vector-copy! v 0 vec n len)
    v))

(define (s64vector-drop-right vec n)
  (let* ((len (s64vector-length vec))
         (rlen (- len n))
         (v (make-s64vector rlen)))
    (s64vector-copy! v 0 vec 0 rlen)
    v))

(define (s64vector-segment vec n)
  (let loop ((r '()) (i 0) (remain (s64vector-length vec)))
    (if (<= remain 0)
      (reverse r)
      (let ((size (min n remain)))
        (loop
         (cons (s64vector-copy vec i (+ i size)) r)
         (+ i size)
         (- remain size))))))

(define (%s64vectors-ref vecs i) (map (lambda (v) (s64vector-ref v i)) vecs))

(define (s64vector-fold kons knil vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((r knil) (i 0))
        (if (= i len) r (loop (kons r (s64vector-ref vec i)) (+ i 1)))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((r knil) (i 0))
        (if (= i len)
          r
          (loop (apply kons r (%s64vectors-ref vecs i)) (+ i 1)))))))

(define (s64vector-fold-right kons knil vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((r knil) (i (- (s64vector-length vec) 1)))
        (if (negative? i) r (loop (kons r (s64vector-ref vec i)) (- i 1)))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((r knil) (i (- len 1)))
        (if (negative? i)
          r
          (loop (apply kons r (%s64vectors-ref vecs i)) (- i 1)))))))

(define (s64vector-map f vec . vecs)
  (if (null? vecs)
    (let* ((len (s64vector-length vec)) (v (make-s64vector len)))
      (let loop ((i 0))
        (unless (= i len)
          (s64vector-set! v i (f (s64vector-ref vec i)))
          (loop (+ i 1))))
      v)
    (let* ((vecs (cons vec vecs))
           (len (apply min (map s64vector-length vecs)))
           (v (make-s64vector len)))
      (let loop ((i 0))
        (unless (= i len)
          (s64vector-set! v i (apply f (%s64vectors-ref vecs i)))
          (loop (+ i 1))))
      v)))

(define (s64vector-map! f vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0))
        (unless (= i len)
          (s64vector-set! vec i (f (s64vector-ref vec i)))
          (loop (+ i 1)))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0))
        (unless (= i len)
          (s64vector-set! vec i (apply f (%s64vectors-ref vecs i)))
          (loop (+ i 1)))))))

(define (s64vector-for-each f vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0))
        (unless (= i len) (f (s64vector-ref vec i)) (loop (+ i 1)))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0))
        (unless (= i len) (apply f (%s64vectors-ref vecs i)) (loop (+ i 1)))))))

(define (s64vector-count pred vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0) (r 0))
        (cond
         ((= i (s64vector-length vec)) r)
         ((pred (s64vector-ref vec i)) (loop (+ i 1) (+ r 1)))
         (else (loop (+ i 1) r)))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0) (r 0))
        (cond
         ((= i len) r)
         ((apply pred (%s64vectors-ref vecs i)) (loop (+ i 1) (+ r 1)))
         (else (loop (+ i 1) r)))))))

(define (s64vector-cumulate f knil vec)
  (let* ((len (s64vector-length vec)) (v (make-s64vector len)))
    (let loop ((r knil) (i 0))
      (unless (= i len)
        (let ((next (f r (s64vector-ref vec i))))
          (s64vector-set! v i next)
          (loop next (+ i 1)))))
    v))

(define (s64vector-take-while pred vec)
  (let* ((len (s64vector-length vec))
         (idx (s64vector-skip pred vec))
         (idx* (if idx idx len)))
    (s64vector-copy vec 0 idx*)))

(define (s64vector-take-while-right pred vec)
  (let* ((len (s64vector-length vec))
         (idx (s64vector-skip-right pred vec))
         (idx* (if idx (+ idx 1) 0)))
    (s64vector-copy vec idx* len)))

(define (s64vector-drop-while pred vec)
  (let* ((len (s64vector-length vec))
         (idx (s64vector-skip pred vec))
         (idx* (if idx idx len)))
    (s64vector-copy vec idx* len)))

(define (s64vector-drop-while-right pred vec)
  (let* ((len (s64vector-length vec))
         (idx (s64vector-skip-right pred vec))
         (idx* (if idx idx -1)))
    (s64vector-copy vec 0 (+ 1 idx*))))

(define (s64vector-index pred vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0))
        (cond
         ((= i len) #f)
         ((pred (s64vector-ref vec i)) i)
         (else (loop (+ i 1))))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0))
        (cond
         ((= i len) #f)
         ((apply pred (%s64vectors-ref vecs i)) i)
         (else (loop (+ i 1))))))))

(define (s64vector-index-right pred vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i (- len 1)))
        (cond
         ((negative? i) #f)
         ((pred (s64vector-ref vec i)) i)
         (else (loop (- i 1))))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i (- len 1)))
        (cond
         ((negative? i) #f)
         ((apply pred (%s64vectors-ref vecs i)) i)
         (else (loop (- i 1))))))))

(define (s64vector-skip pred vec . vecs)
  (if (null? vecs)
    (s64vector-index (lambda (x) (not (pred x))) vec)
    (apply s64vector-index (lambda xs (not (apply pred xs))) vec vecs)))

(define (s64vector-skip-right pred vec . vecs)
  (if (null? vecs)
    (s64vector-index-right (lambda (x) (not (pred x))) vec)
    (apply s64vector-index-right (lambda xs (not (apply pred xs))) vec vecs)))

(define (s64vector-any pred vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0))
        (cond
         ((= i len) #f)
         ((pred (s64vector-ref vec i)))
         (else (loop (+ i 1))))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0))
        (cond
         ((= i len) #f)
         ((apply pred (%s64vectors-ref vecs i)))
         (else (loop (+ i 1))))))))

(define (s64vector-every pred vec . vecs)
  (if (null? vecs)
    (let ((len (s64vector-length vec)))
      (let loop ((i 0) (last #t))
        (cond
         ((= i len) last)
         ((pred (s64vector-ref vec i)) => (lambda (r) (loop (+ i 1) r)))
         (else #f))))
    (let* ((vecs (cons vec vecs)) (len (apply min (map s64vector-length vecs))))
      (let loop ((i 0) (last #t))
        (cond
         ((= i len) last)
         ((apply pred (%s64vectors-ref vecs i))
          =>
          (lambda (r) (loop (+ i 1) r)))
         (else #f))))))

(define (s64vector-partition pred vec)
  (let* ((len (s64vector-length vec))
         (cnt (s64vector-count pred vec))
         (r (make-s64vector len)))
    (let loop ((i 0) (yes 0) (no cnt))
      (cond
       ((= i len) r)
       ((pred (s64vector-ref vec i))
        (s64vector-set! r yes (s64vector-ref vec i))
        (loop (+ i 1) (+ yes 1) no))
       (else
        (s64vector-set! r no (s64vector-ref vec i))
        (loop (+ i 1) yes (+ no 1)))))))

(define (s64vector-filter pred vec)
  (let* ((len (s64vector-length vec))
         (cnt (s64vector-count pred vec))
         (r (make-s64vector cnt)))
    (let loop ((i 0) (j 0))
      (cond
       ((= i len) r)
       ((pred (s64vector-ref vec i))
        (s64vector-set! r j (s64vector-ref vec i))
        (loop (+ i 1) (+ j 1)))
       (else (loop (+ i 1) j))))))

(define (s64vector-remove pred vec)
  (s64vector-filter (lambda (x) (not (pred x))) vec))

(define (s64vector-swap! vec i j)
  (let ((ival (s64vector-ref vec i)) (jval (s64vector-ref vec j)))
    (s64vector-set! vec i jval)
    (s64vector-set! vec j ival)))

(define (s64vector-fill! vec fill (start 0) (end (s64vector-length vec)))
  (unless (= start end)
    (s64vector-set! vec start fill)
    (s64vector-fill! vec fill (+ start 1) end)))

(define (s64vector-reverse! vec (start 0) (end (s64vector-length vec)))
  (let loop ((i start) (j (- end 1)))
    (when (< i j) (s64vector-swap! vec i j) (loop (+ i 1) (- j 1)))))

(define (s64vector-unfold! f vec start end seed)
  (let loop ((i start) (seed seed))
    (when (< i end)
      (let-values (((elt seed) (f i seed)))
        (s64vector-set! vec i elt)
        (loop (+ i 1) seed)))))

(define (s64vector-unfold-right! f vec start end seed)
  (let loop ((i (- end 1)) (seed seed))
    (when (>= i start)
      (let-values (((elt seed) (f i seed)))
        (s64vector-set! vec i elt)
        (loop (- i 1) seed)))))

(define (reverse-s64vector->list vec (start 0) (end (s64vector-length vec)))
  (let loop ((i start) (r '()))
    (if (= i end) r (loop (+ 1 i) (cons (s64vector-ref vec i) r)))))

(define (reverse-list->s64vector list)
  (let* ((len (length list)) (r (make-s64vector len)))
    (let loop ((i 0) (list list))
      (cond
       ((= i len) r)
       (else
        (s64vector-set! r (- len i 1) (car list))
        (loop (+ i 1) (cdr list)))))))

(define (s64vector->vector vec (start 0) (end (s64vector-length vec)))
  (let* ((len (- end start)) (r (make-vector len)))
    (let loop ((i start) (o 0))
      (cond
       ((= i end) r)
       (else (vector-set! r o (s64vector-ref vec i)) (loop (+ i 1) (+ o 1)))))))

(define (vector->s64vector vec (start 0) (end (vector-length vec)))
  (let* ((len (- end start)) (r (make-s64vector len)))
    (let loop ((i start) (o 0))
      (cond
       ((= i end) r)
       (else (s64vector-set! r o (vector-ref vec i)) (loop (+ i 1) (+ o 1)))))))

(define (make-s64vector-generator vec (start 0) (end (s64vector-length vec)))
  (lambda ()
    (if (>= start end)
      eof
      (let ((next (s64vector-ref vec start))) (set! start (+ start 1)) next))))

(define (write-s64vector vec (port (current-output-port)))
  (fprintf port "#~A(" 's64)
  (let ((last (- (s64vector-length vec) 1)))
    (let loop ((i 0))
      (cond
       ((= i last) (write (s64vector-ref vec i) port) (display ")" port))
       (else
        (write (s64vector-ref vec i) port)
        (display " " port)
        (loop (+ i 1)))))))

(define (s64vector< vec1 vec2)
  (let ((len1 (s64vector-length vec1)) (len2 (s64vector-length vec2)))
    (cond
     ((< len1 len2) #t)
     ((> len1 len2) #f)
     (else
      (let loop ((i 0))
        (cond
         ((= i len1) #f)
         ((< (s64vector-ref vec1 i) (s64vector-ref vec2 i)) #t)
         ((> (s64vector-ref vec1 i) (s64vector-ref vec2 i)) #f)
         (else (loop (+ i 1)))))))))

(define (s64vector-hash vec)
  (let ((len (min 256 (s64vector-length vec))))
    (let loop ((i 0) (r 0))
      (if (= i len)
        (abs (floor (real-part (inexact->exact r))))
        (loop (+ i 1) (+ r (s64vector-ref vec i)))))))

(define s64vector-comparator
  (make-comparator s64vector? s64vector= s64vector< s64vector-hash))

